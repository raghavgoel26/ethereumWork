contract('manageRawMaterial', function(accounts) {
  it("should assert true", function(done) {
    var manage_raw_material = manageRawMaterial.deployed();
    assert.isTrue(true);
    done();
  });
});
